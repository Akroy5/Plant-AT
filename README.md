# Plant‐AT: Hybrid Attention Model for Plant Disease Detection

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)  
[![Python Version](https://img.shields.io/badge/python-3.8%2B-green.svg)]()

> **Plant-AT** is a real-time deep learning architecture designed to classify plant species and diagnose diseases from leaf images with high accuracy. The model is optimized for **real-time deployment** on edge devices (Jetson AGX Orin), balancing accuracy, efficiency, and memory footprint.
 
## Repository Structure

- `CODE/`: Model and training code (e.g. `PlantAT3L.py`).  
- `PAPER/`: Research paper PDF (`Plant_AT.pdf`).  
- `IMAGES/`: Model architecture image and figures (`modellast.png`).  
- `DATA/`: Raw data files (**ignored by Git**).  
- `RESULTS/`: Output results and logs (**ignored by Git**).  

## Getting Started

1. Install required Python packages (see `requirements.txt`).  
2. Place your dataset files in the `DATA/` folder (not committed).  
3. Run the model script:





